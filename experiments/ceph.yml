# Author: Michael Sevilla
# Bring up Ceph and get its status

- hosts: all
  roles: [cleanup]

#- hosts: Builder
#  roles: [ceph-build]
#  vars:  [BUILD_THREADS: 64, SHA1_OR_REF: "master", GIT_URL: "",  CEPH_PKGS: ""]

- hosts: Workers
  vars:  [STARTUP: "singlenode.sh"]
  roles: [ceph]

- hosts: Builder
  roles: [ceph-client]
  tasks:
    - command: docker exec ceph-client ceph -s
      register: result
      async: 10
      poll: 1
    - debug: var=result.stdout_lines
