---
- name: run the hadoop namenode docker image
  docker:
    image: "{{ hadoop_docker_username }}/{{ hadoop_docker_imagename }}"
    name: "{{ container_name_namenode }}"
    net: "host"
    state: "running"
    env: "HADOOP_DAEMON=NAMENODE"
    volumes: "/etc/hadoop:/etc/hadoop"
    detach: true
    tty: true
