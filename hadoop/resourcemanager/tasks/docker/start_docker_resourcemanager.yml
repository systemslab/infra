---
- name: run the hadoop resourcemanager docker image
  docker:
    image: "{{ hadoop_docker_username }}/{{ hadoop_docker_imagename }}"
    name: "{{ container_name_resourcemanager }}"
    net: "host"
    state: "running"
    env: "HADOOP_DAEMON=RESOURCEMANAGER"
    volumes: "/etc/hadoop:/etc/hadoop"
    detach: true
    tty: true
