# Author: Michael Sevilla
# Package up the source code in preparation for network transfer

#- command: docker exec cephdev-build /bin/bash -c "cd /ceph; debuild -b -nc -j{{ threads }} -fakeroot -us -uc"
#
